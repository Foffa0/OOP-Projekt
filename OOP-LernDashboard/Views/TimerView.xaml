<UserControl x:Class="OOP_LernDashboard.Views.TimerView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:local="clr-namespace:OOP_LernDashboard.Views"
         xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
         mc:Ignorable="d" 
         xmlns:hc="https://handyorg.github.io/handycontrol"
         d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource WindowTheme}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Foreground="{StaticResource TextColor}" Text="Timer" Style="{StaticResource Title}"></TextBlock>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Height="220" VerticalAlignment="Top">
            <Border Background="{StaticResource SecondaryColor}" CornerRadius="10" Padding="10" Margin="10,0,10,0"  Height="220">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Timer 1" Foreground="{StaticResource TextColor}" Style="{StaticResource Headline}" Margin="0"></TextBlock>

                    <StackPanel VerticalAlignment="Center" Grid.Row="1" Orientation="Horizontal">
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50"></RowDefinition>
                                <RowDefinition Height="60"></RowDefinition>
                            </Grid.RowDefinitions>

                            
                            <StackPanel Grid.Row="0" Orientation="Horizontal">

                                <hc:TextBox x:Name="hours" Text="{Binding Hours}" Width="60" TextAlignment="Center"  />

                                <TextBlock FontSize="20" Margin="3,0,3,0" Foreground="{StaticResource TextColor}">:</TextBlock>

                                <hc:TextBox x:Name="minutes" Text="{Binding Minutes}" Width="60" TextAlignment="Center"/>

                                <TextBlock FontSize="20" Margin="3,0,3,0" Foreground="{StaticResource TextColor}">:</TextBlock>

                                <!--</hc:TextBox>hc:InfoElement.Placeholder="0"-->
                                <hc:TextBox x:Name="seconds" Text="{Binding Seconds}"  Width="60" TextAlignment="Center"/>

                            </StackPanel>

                            <Button Grid.Row="1" Style="{StaticResource PrimaryButton}" Width="80" Margin="0,10,0,0">
                                
                            </Button>

                        </Grid>



                    </StackPanel>

                </Grid>
            </Border>

            <Border Background="{StaticResource SecondaryColor}" CornerRadius="10" Padding="10" Margin="10,0,10,0"  Height="220">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!--
                    <Slider VerticalAlignment="Center" Value="40" Maximum="100"/>
                    -->
                    <Button Style="{StaticResource PrimaryButton}" Content="{Binding Timer}" Width="60" Command="{Binding SetTimerCommand}" >

                    </Button>
                    <hc:CircleProgressBar Grid.Row="1" Value="{Binding BarValue}" ShowText="False" Width="160" Height="160" ArcThickness="2" />
                </Grid>
            </Border>

        </StackPanel>
        
        
    </Grid>

</UserControl>