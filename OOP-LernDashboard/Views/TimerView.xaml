<UserControl x:Class="OOP_LernDashboard.Views.TimerView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:local="clr-namespace:OOP_LernDashboard.Views"
         xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
         mc:Ignorable="d" 
         d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource WindowTheme}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Foreground="{StaticResource TextColor}" Text="Timer" Style="{StaticResource Title}"></TextBlock>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Height="200" VerticalAlignment="Top" >
            <Border Background="{StaticResource SecondaryColor}" CornerRadius="10" Padding="10" Margin="10,0,10,0" Width="260" Height="200">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Timer 1" Foreground="{StaticResource TextColor}" Style="{StaticResource Headline}" Margin="0"></TextBlock>
                    
                    <StackPanel VerticalAlignment="Center" Grid.Row="1" Orientation="Horizontal">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal">

                                <TextBox Width="70">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                            <Style.Resources>
                                                <VisualBrush x:Key="CueBannerBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <Label Content="Stunden" Foreground="LightGray" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Style.Resources>
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="{x:Static sys:String.Empty}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="Text" Value="{x:Null}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                    <Setter Property="Background" Value="White" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <TextBlock FontSize="20" Margin="3,0,3,0" Foreground="{StaticResource TextColor}">:</TextBlock>

                                <TextBox Width="70" TextChanged="TextBox_TextChanged">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                            <Style.Resources>
                                                <VisualBrush x:Key="CueBannerBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <Label Content="Minuten" Foreground="LightGray" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                                
                                            </Style.Resources>
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="{x:Static sys:String.Empty}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="Text" Value="{x:Null}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                    <Setter Property="Background" Value="White" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                                <TextBlock FontSize="20" Margin="3,0,3,0" Foreground="{StaticResource TextColor}">:</TextBlock>

                                <TextBox Width="80">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                            <Style.Resources>
                                                <VisualBrush x:Key="CueBannerBrush" AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <Label Content="Sekunden" Foreground="LightGray" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Style.Resources>
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="{x:Static sys:String.Empty}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="Text" Value="{x:Null}">
                                                    <Setter Property="Background" Value="{StaticResource CueBannerBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocused" Value="True">
                                                    <Setter Property="Background" Value="White" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>

                            </StackPanel>

                            <Button Grid.Row="1" Style="{StaticResource PrimaryButton}" Content="Add" Margin="0,10,0,0" Height="35"></Button>

                        </Grid>
                        
                        
                        
                    </StackPanel>
                    
                </Grid>
            </Border>

            <Border Background="{StaticResource SecondaryColor}" CornerRadius="10" Padding="10" Margin="10,0,10,0" Width="260" Height="200">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Timer 1" Foreground="{StaticResource TextColor}" Style="{StaticResource Headline}" Margin="0"></TextBlock>

                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1" Orientation="Horizontal">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="80"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Orientation="Horizontal">

                                <TextBlock FontSize="40" VerticalAlignment="Center" FontWeight="Bold" Margin="3,0,3,0" Foreground="{StaticResource TextColor}">20</TextBlock>

                                <TextBlock FontSize="20" VerticalAlignment="Center" Margin="3,0,3,0" Foreground="{StaticResource TextColor}" >:</TextBlock>

                                <TextBlock FontSize="40" VerticalAlignment="Center" FontWeight="Bold" Margin="3,0,3,0" Foreground="{StaticResource TextColor}">56</TextBlock>

                                <TextBlock FontSize="20" VerticalAlignment="Center" Margin="3,0,3,0" Foreground="{StaticResource TextColor}">:</TextBlock>

                                <TextBlock FontSize="40" VerticalAlignment="Center" FontWeight="Bold" Margin="3,0,3,0" Foreground="{StaticResource TextColor}">04</TextBlock>

                            </StackPanel>

                            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Grid.Row="1" Background="{StaticResource SecondaryColor}" BorderBrush="{StaticResource PrimaryColor}" BorderThickness="2" Content="Start" Margin="5,0,5,0" Width="50" Height="40"></Button>
                                <Button Grid.Row="1" Style="{StaticResource PrimaryButton}" Content="Stop" Margin="5,0,5,0" Padding="0" Width="50" Height="40"></Button>
                            </StackPanel>

                        </Grid>



                    </StackPanel>

                </Grid>
            </Border>


        </StackPanel>
    </Grid>

</UserControl>